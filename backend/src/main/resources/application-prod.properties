# Production profile: run with --spring.profiles.active=prod behind HTTPS.
# Send the session cookie over HTTPS only.
server.servlet.session.cookie.secure=true
app.seed.enabled=false
# A real deployment would also point spring.datasource.* at a persistent database and use
# spring.jpa.hibernate.ddl-auto=validate with versioned migrations.
